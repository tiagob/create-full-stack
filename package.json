{
  "name": "create-boilerplate-app",
  "version": "0.1.0",
  "description": "Configuration and scripts for Create Boilerplate App.",
  "devDependencies": {
    "@types/node": "^12.7.11",
    "commander": "^2.19.0",
    "husky": "^1.3.1",
    "prettier": "1.16.4",
    "pretty-quick": "^1.10.0",
    "typescript": "^3.3.3"
  },
  "scripts": {
    "init": "tsc && node ./build/init.js",
    "init-debug": "tsc && node --inspect-brk ./build/init.js"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  },
  "license": "MIT"
}
